---
description: "操作约束与系统架构定位（C1-C5）。涵盖：换方案先确认、有疑问多问、程序唯一入口 pipeline、两种使用方式、规则集作用域（工程纪律：只管编码，不管翻译业务）、读取一律走本地文件系统（读文档/代码/videos 一律本地目录，不走 git 视角）。"
alwaysApply: true
enabled: true
updatedAt: 2026-09-16T00:00:00.000Z
---

# 操作约束与系统架构

本目录（`.codebuddy/rules/`）是**项目工程纪律**，规定「怎么改这个项目的代码」。
分工固定：**业务规则 → `AGENTS.md`；工程约束 → 本目录。** 本目录不写翻译业务规则，
`AGENTS.md` 不写编码规范；边界只在本文件说明，不在别处重复。

## C1：换方案必须先确认
- 凡涉及「换方案 / 换工具 / 改默认值 / 换实现路径」的决定，**必须先向用户确认，不得自行拍板**。
- 认为当前方案有问题 → 停下说明并请示；❌ 默默换成另一个你更喜欢的方案。

## C2：有疑问宁可多问
- 不得替用户做主；需求有歧义先澄清再动手。

## C3：程序唯一入口 = pipeline
- 入口：`pipeline`（`run` / `generate` / `verify` 为原语）。
- 两种使用方式：
  - **有翻译 agent**：用户说「我要翻译这个视频」→ agent 调 `pipeline` 走流程，agent 只负责翻译 + 语义回读 + 指向入口（协议见 `AGENTS.md`）；
  - **无 agent**：用户/LLM 直接调 `pipeline`。
- 状态机 = `pipeline_def.STAGES`（纯数据）+ `pipeline.py`（引擎）+ `state.py`（`<base>.vt_state.json`）。

## C4：规则集作用域 —— 只管编码，不管翻译业务
- 本规则集约束**一切改本仓库代码的行为**：SDD / TDD / 数据契约 / 依赖 / 命令 harness / 系统架构。
  - **含翻译流程的代码实现**：transcribe / translate / merge / fill_gaps / generate / verify
    各 stage、产物契约、闸门、状态机的改动，一律受本规则集约束，契约照旧写 `docs/specs/` + `docs/adr/`。
- **不管翻译业务本身**：翻译风格 / persona / 语感 / 语义回读判定标准 / agent 与 task 文件的
  交互协议 → 写进 `AGENTS.md`。
- 用户下翻译任务（「翻译这个视频」）时**照 `AGENTS.md` 执行**，不受本规则集缺翻译条款影响。

## C5：读取一律走本地文件系统，不走 git 视角

需要读**项目内文档 / 代码 / 产物 / `videos/`** 时，一律直接读**工作区本地目录**
（文件系统真实路径）；**不得**用 git 追踪范围判断「有没有这个东西」。

- **不在 git 里 ≠ 不存在**。`.gitignore` 覆盖的都是真实存在且可读的本地内容：
  `.codebuddy/`（工程规则 + 开发计划）、`videos/`（输入视频）、`outputs/`、`models/`、
  `tools/`、`.env*`、`docs/golden/`。
- ❌ 拿 `git ls-files` / 「git status 里没有」当作存在性依据。
- ❌ 从**带过滤条件的枚举结果**反推否定结论：如 `Get-ChildItem -Filter *.md` 没看到就
  断言某目录「不存在」——`.mdc` 会被该过滤器整个漏掉（**真实踩过**）。
- ✅ 否定性判断（「不存在」「没有」「已删除」）必须先**正向验证**：
  `Test-Path` / 显式列目录 / 加 `-Force` 含隐藏项；一次验不到就换方式再验，不许直接下结论。
- ✅ 枚举时覆盖非默认扩展名与隐藏目录（`.mdc` / `.codebuddy/` / `.clinerules/` / `.github/`）。
- ✅ 读 `videos/` 下的视频与产物同样走本地路径（该目录永不进 git，git 视角下是空的）。

## 禁止行为
- ❌ 以 git 追踪范围 / 带过滤条件的枚举结果判断文件或目录「不存在」
- ❌ 未经确认切换方案 / 工具 / 默认值 / 实现路径
- ❌ 把编码规范写进 `AGENTS.md`；把翻译业务规则写进本目录
- ❌ 绕过 `pipeline` 入口自行编排流程
- ❌ 以「我是编码助手 / 规则集没写翻译流程」为由拒绝或回避翻译任务
- ❌ 以「翻译流程归 `AGENTS.md`」为由，跳过 SDD/TDD 直接改翻译流程的代码
